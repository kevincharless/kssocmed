import React from 'react';
import { Container } from './Title.elements';

const Title = ({ title }) => {
    return (
        <Container>
            {title}
        </Container>
    )
}

export default Title
